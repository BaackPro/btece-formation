
cd "C:\btece-formation"
git add --all
git commit -m "Auto-update $(Get-Date -Format 'dd/MM/yyyy HH:mm')"
git push origin main


git push origin HEAD

Santanda



cd "C:\Users\prince CHABI\Desktop\btece"
# D'abord récupérer les changements distants
git pull origin main
# Ensuite ajouter vos modifications
git add --all
# Créer un commit
git commit -m "Auto-update $(Get-Date -Format 'dd/MM/yyyy HH:mm')"
# Finalement pousser vers GitHub
git push origin main



cd "C:\Users\prince CHABI\Desktop\btece"
git add --all
git commit -m "Auto-update $(Get-Date -Format 'dd/MM/yyyy HH:mm')"
git push origin main